name: SLSA L3 DevSecOps Secure Supply Chain

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]
  workflow_dispatch:

env:
  IMAGE_REGISTRY: ghcr.io
  IMAGE_REPO: ${{ github.repository }}

permissions:
  contents: read

jobs:
  # ============================================
  # Stage 1: Prepare & Verify Source
  # ============================================
  prepare:
    name: "1️⃣ Prepare & Verify Source"
    runs-on: ubuntu-latest
    permissions:
      contents: read

    outputs:
      build_start_time: ${{ steps.start-time.outputs.time }}
      version: ${{ steps.version.outputs.version }}
      image_name: ${{ steps.image-info.outputs.image_name }}
      image_tag: ${{ steps.image-info.outputs.image_tag }}

    steps:
      - name: "⏱️ Record Start Time"
        id: start-time
        run: echo "time=$(date +%s)" >> $GITHUB_OUTPUT

      - name: "📋 Checkout Repository"
        uses: actions/checkout@v4

      - name: "🏷️ Generate Version"
        id: version
        run: |
          VERSION="v1.0.${{ github.run_number }}-${GITHUB_SHA::8}"
          echo "version=${VERSION}" >> $GITHUB_OUTPUT
          echo "🏷️ Version: ${VERSION}"

      - name: "🏷️ Set Image Information"
        id: image-info
        run: |
          IMAGE_NAME="${{ env.IMAGE_REGISTRY }}/${{ env.IMAGE_REPO }}"
          IMAGE_TAG="${{ steps.version.outputs.version }}"
          echo "image_name=${IMAGE_NAME}" >> $GITHUB_OUTPUT
          echo "image_tag=${IMAGE_TAG}" >> $GITHUB_OUTPUT
          echo "🏷️ Image Name: ${IMAGE_NAME}"
          echo "🏷️ Image Tag: ${IMAGE_TAG}"

      - name: "✅ Verify Source Files"
        run: |
          if [[ ! -d app ]]; then
            echo "❌ Error: app directory missing."
            exit 1
          fi
          echo "✅ Source files verified"

  # ============================================
  # Stage 2: Build Container Image (SLSA L3)
  # ============================================
  build-container:
    name: "2️⃣ Build Container Image (SLSA L3)"
    needs: [prepare]
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
      id-token: write
      attestations: write

    outputs:
      image_digest: ${{ steps.build.outputs.digest }}
      image_ref: ${{ steps.image-ref.outputs.ref }}

    steps:
      - name: "📋 Checkout Repository"
        uses: actions/checkout@v4

      - name: "🔐 Log in to GitHub Container Registry"
        uses: docker/login-action@v3
        with:
          registry: ${{ env.IMAGE_REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: "🗝️ Set up Docker Buildx"
        uses: docker/setup-buildx-action@v3

      - name: "🐳 Build and Push Docker Image"
        id: build
        uses: docker/build-push-action@v5
        with:
          context: ./app
          file: ./app/Dockerfile
          push: true
          tags: ${{ needs.prepare.outputs.image_name }}:${{ needs.prepare.outputs.image_tag }}
          platforms: linux/amd64
          provenance: false
          sbom: false
          build-args: |
            VERSION=${{ needs.prepare.outputs.version }}
            BUILD_DATE=${{ needs.prepare.outputs.build_start_time }}
            VCS_REF=${{ github.sha }}

      - name: "🔍 Get Image Digest"
        id: image-ref
        run: |
          DIGEST="${{ steps.build.outputs.digest }}"
          IMAGE_REF="${{ needs.prepare.outputs.image_name }}@${DIGEST}"
          echo "digest=${DIGEST}" >> $GITHUB_OUTPUT
          echo "ref=${IMAGE_REF}" >> $GITHUB_OUTPUT
          echo "🔍 Image Digest: ${DIGEST}"
          echo "🔍 Image Ref: ${IMAGE_REF}"

      - name: "🔐 Install Cosign"
        uses: sigstore/cosign-installer@v3

      - name: "✏️ Sign Container Image (SLSA Requirement)"
        env:
          COSIGN_EXPERIMENTAL: 1
        run: |
          cosign sign --yes "${{ steps.image-ref.outputs.ref }}"

      - name: "📦 Generate SBOM with Syft"
        run: |
          curl -sSfL https://raw.githubusercontent.com/anchore/syft/main/install.sh | sh -s -- -b /usr/local/bin
          syft "${{ steps.image-ref.outputs.ref }}" -o cyclonedx-json > bom.json

      - name: "🔎 Attach SBOM Attestation (SLSA Requirement)"
        env:
          COSIGN_EXPERIMENTAL: 1
        run: |
          cosign attest --yes --type cyclonedx --predicate bom.json "${{ steps.image-ref.outputs.ref }}"

      - name: "💾 Upload SBOM Artifact"
        uses: actions/upload-artifact@v4
        with:
          name: sbom
          path: bom.json
          retention-days: 90

      - name: "🔐 Verify Signature (SLSA L3 Verification)"
        env:
          COSIGN_EXPERIMENTAL: 1
        run: |
          cosign verify \
            --certificate-oidc-issuer https://token.actions.githubusercontent.com \
            --certificate-identity-regexp "^https://github.com/${{ github.repository }}/.*" \
            "${{ steps.image-ref.outputs.ref }}"

  # ============================================
  # Stage 3a: Vulnerability Scanning (Parallel)
  # ============================================
  scan-vulnerabilities:
    name: "3️⃣a SBOM & Vulnerability Scan"
    runs-on: ubuntu-latest
    needs: [build-container]
    permissions:
      contents: read
      packages: read
    outputs:
      sbom_components_count: ${{ steps.sbom-stats.outputs.count }}
      vulnerabilities_found: ${{ steps.vuln-stats.outputs.total }}
      critical_count: ${{ steps.vuln-stats.outputs.critical }}
      high_count: ${{ steps.vuln-stats.outputs.high }}
      medium_count: ${{ steps.vuln-stats.outputs.medium }}
      low_count: ${{ steps.vuln-stats.outputs.low }}

    steps:
      - name: "🔐 Log in to GHCR"
        uses: docker/login-action@v3
        with:
          registry: ${{ env.IMAGE_REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: "⬇️ Pull Signed Image"
        run: docker pull "${{ needs.build-container.outputs.image_ref }}"

      - name: "⬇️ Download SBOM"
        uses: actions/download-artifact@v4
        with:
          name: sbom

      - name: "🔍 Install Grype Scanner"
        run: |
          curl -sSfL https://raw.githubusercontent.com/anchore/grype/main/install.sh | sh -s -- -b /usr/local/bin

      - name: "📈 Extract SBOM Statistics"
        id: sbom-stats
        run: |
          COMPONENT_COUNT=$(jq '.components | length' bom.json 2>/dev/null || echo "0")
          echo "count=${COMPONENT_COUNT}" >> $GITHUB_OUTPUT
          echo "📦 SBOM Components: ${COMPONENT_COUNT}"

      - name: "🔍 Scan for CVE Vulnerabilities (Grype)"
        run: |
          grype sbom:bom.json -o json > vulnerabilities.json || echo '{"matches": []}' > vulnerabilities.json

      - name: "📉 Extract CVE Vulnerability Statistics"
        id: vuln-stats
        run: |
          echo "📊 Analyzing CVE vulnerability scan results..."
          
          if [ -f vulnerabilities.json ]; then
            # Get ALL severities present in the data for debugging
            echo "🔍 Raw severity distribution from Grype:"
            jq '[.matches[]?.vulnerability?.severity // "null"] | group_by(.) | map({severity: .[0], count: length}) | sort_by(.severity)' vulnerabilities.json || echo "  Unable to parse"
            
            # Count unique CVEs by severity (robust against null/empty)
            CRITICAL=$(jq '[.matches[] | select(.vulnerability?.severity | test("critical"; "i"))] | unique_by(.vulnerability.id) | length' vulnerabilities.json 2>/dev/null || echo "0")
            HIGH=$(jq '[.matches[] | select(.vulnerability?.severity | test("high"; "i"))] | unique_by(.vulnerability.id) | length' vulnerabilities.json 2>/dev/null || echo "0")
            MEDIUM=$(jq '[.matches[] | select(.vulnerability?.severity | test("medium"; "i"))] | unique_by(.vulnerability.id) | length' vulnerabilities.json 2>/dev/null || echo "0")
            LOW=$(jq '[.matches[] | select(.vulnerability?.severity | test("low|negligible|unknown"; "i"))] | unique_by(.vulnerability.id) | length' vulnerabilities.json 2>/dev/null || echo "0")
            
            # Calculate total as sum (more reliable)
            TOTAL=$((CRITICAL + HIGH + MEDIUM + LOW))
            
            # Debug: Show if there are unmatched severities
            UNMATCHED=$(jq '[.matches[] | select(.vulnerability?.severity | test("critical|high|medium|low|negligible|unknown"; "i") | not)] | length' vulnerabilities.json 2>/dev/null || echo "0")
            if [ "$UNMATCHED" -gt 0 ]; then
              echo "⚠️  Found $UNMATCHED matches with non-standard severities:"
              jq '[.matches[] | select(.vulnerability?.severity | test("critical|high|medium|low|negligible|unknown"; "i") | not) | .vulnerability.severity] | unique' vulnerabilities.json 2>/dev/null || echo "  Unable to list"
            fi
          else
            echo "⚠️  vulnerabilities.json not found, setting all counts to 0"
            TOTAL=0; CRITICAL=0; HIGH=0; MEDIUM=0; LOW=0
          fi
          
          echo "total=${TOTAL}" >> $GITHUB_OUTPUT
          echo "critical=${CRITICAL}" >> $GITHUB_OUTPUT
          echo "high=${HIGH}" >> $GITHUB_OUTPUT
          echo "medium=${MEDIUM}" >> $GITHUB_OUTPUT
          echo "low=${LOW}" >> $GITHUB_OUTPUT
          
          echo "🔍 Final Counts - CVE Vulnerabilities: ${TOTAL} (C:${CRITICAL} H:${HIGH} M:${MEDIUM} L:${LOW})"

      - name: "💾 Upload Vulnerability Results"
        uses: actions/upload-artifact@v4
        with:
          name: vulnerability-results
          path: vulnerabilities.json
          retention-days: 90

  # ============================================
  # Stage 3b: SAST Scan (Parallel)
  # ============================================
  scan-sast:
    name: "3️⃣b SAST Code Scan"
    runs-on: ubuntu-latest
    needs: [prepare]
    permissions:
      contents: read
    outputs:
      sast_issues_found: ${{ steps.sast-stats.outputs.total }}
      sast_critical: ${{ steps.sast-stats.outputs.critical }}
      sast_high: ${{ steps.sast-stats.outputs.high }}
      sast_medium: ${{ steps.sast-stats.outputs.medium }}
      sast_low: ${{ steps.sast-stats.outputs.low }}

    steps:
      - name: "📋 Checkout Repository"
        uses: actions/checkout@v4

      - name: "🔬 Install Semgrep"
        run: |
          python3 -m pip install semgrep

      - name: "🔬 Run Semgrep SAST Scan"
        run: |
          semgrep scan --config=auto --exclude=".github/workflows/*.yml" --json --output=semgrep.json || true

      - name: "📊 Extract SAST Statistics"
        id: sast-stats
        run: |
          if [ -f semgrep.json ]; then
            TOTAL=$(jq '.results | length' semgrep.json 2>/dev/null || echo "0")
            CRITICAL=$(jq '[.results[] | select(.extra.severity == "ERROR")] | length' semgrep.json 2>/dev/null || echo "0")
            HIGH=$(jq '[.results[] | select(.extra.severity == "WARNING")] | length' semgrep.json 2>/dev/null || echo "0")
            MEDIUM=$(jq '[.results[] | select(.extra.severity == "INFO")] | length' semgrep.json 2>/dev/null || echo "0")
            LOW=$(jq '[.results[] | select(.extra.severity == "NOTE")] | length' semgrep.json 2>/dev/null || echo "0")
          else
            TOTAL=0; CRITICAL=0; HIGH=0; MEDIUM=0; LOW=0
          fi
          echo "total=${TOTAL}" >> $GITHUB_OUTPUT
          echo "critical=${CRITICAL}" >> $GITHUB_OUTPUT
          echo "high=${HIGH}" >> $GITHUB_OUTPUT
          echo "medium=${MEDIUM}" >> $GITHUB_OUTPUT
          echo "low=${LOW}" >> $GITHUB_OUTPUT
          echo "🔬 SAST Issues: ${TOTAL} (C:${CRITICAL} H:${HIGH} M:${MEDIUM} L:${LOW})"

      - name: "💾 Upload SAST Results"
        uses: actions/upload-artifact@v4
        with:
          name: sast-results
          path: semgrep.json
          retention-days: 90

  # ============================================
  # Stage 3c: Secret Scanning (Parallel)
  # ============================================
  scan-secrets:
    name: "3️⃣c Secret Detection"
    runs-on: ubuntu-latest
    needs: [prepare]
    permissions:
      contents: read
    outputs:
      secrets_found: ${{ steps.secret-stats.outputs.secrets }}

    steps:
      - name: "📋 Checkout Repository"
        uses: actions/checkout@v4

      - name: "🔐 Install TruffleHog"
        run: |
          curl -sSfL https://raw.githubusercontent.com/trufflesecurity/trufflehog/main/scripts/install.sh | sh -s -- -b /usr/local/bin

      - name: "🔐 Run TruffleHog Secret Scan"
        run: |
          trufflehog filesystem . --json > trufflehog.json || true

      - name: "📊 Extract Secret Statistics"
        id: secret-stats
        run: |
          if [ -f trufflehog.json ]; then
            SECRETS=$(jq '[.[] | select(.Verified == true)] | length' trufflehog.json 2>/dev/null || echo "0")
            if [ "$SECRETS" -eq 0 ]; then
              TOTAL_FINDINGS=$(wc -l < trufflehog.json || echo "0")
              echo "⚠️ Unverified secrets: $TOTAL_FINDINGS (reporting 0)"
            fi
          else
            SECRETS=0
          fi
          echo "secrets=${SECRETS}" >> $GITHUB_OUTPUT
          echo "🔐 Verified Secrets: ${SECRETS}"

      - name: "💾 Upload Secret Scan Results"
        uses: actions/upload-artifact@v4
        with:
          name: secret-scan-results
          path: trufflehog.json
          retention-days: 90

  # ============================================
  # Stage 4: Policy Enforcement (Waits for all scans)
  # ============================================
  policy-enforcement:
    name: "4️⃣ Security Policy Enforcement (OPA)"
    runs-on: ubuntu-latest
    needs: [prepare, build-container, scan-vulnerabilities, scan-sast, scan-secrets]
    permissions:
      contents: read

    outputs:
      policy_start_time: ${{ steps.policy-start.outputs.time }}
      policy_end_time: ${{ steps.policy-end.outputs.time }}
      policy_result: ${{ steps.opa_eval.outputs.passed }}
      policy_violations: ${{ steps.opa_eval.outputs.violations }}
      policy_decision: ${{ steps.opa_eval.outputs.decision }}

    steps:
      - name: "⏱️ Record Policy Check Start"
        id: policy-start
        run: echo "time=$(date +%s)" >> $GITHUB_OUTPUT

      - name: "📋 Checkout Repository (for policy files)"
        uses: actions/checkout@v4

      - name: "⬇️ Download Security Reports"
        uses: actions/download-artifact@v4
        with:
          name: sbom

      - name: "⬇️ Download Vulnerability Results"
        uses: actions/download-artifact@v4
        with:
          name: vulnerability-results

      - name: "⬇️ Download SAST Results"
        uses: actions/download-artifact@v4
        with:
          name: sast-results

      - name: "⬇️ Download Secret Scan Results"
        uses: actions/download-artifact@v4
        with:
          name: secret-scan-results

      - name: "🛡️ Install OPA (Open Policy Agent)"
        run: |
          curl -L -o opa https://github.com/open-policy-agent/opa/releases/latest/download/opa_linux_amd64
          chmod +x opa
          sudo mv opa /usr/local/bin
          opa version

      - name: "📋 Create OPA Input File"
        run: |
          cat > input.json << EOF
          {
            "image": {
              "ref": "${{ needs.build-container.outputs.image_ref }}",
              "digest": "${{ needs.build-container.outputs.image_digest }}"
            },
            "slsa_build": {
              "level": 3,
              "provenance_verified": true,
              "hermetic_build": true,
              "signed": true
            },
            "sbom": {
              "components_count": ${{ needs.scan-vulnerabilities.outputs.sbom_components_count }}
            },
            "vulnerabilities": {
              "total": ${{ needs.scan-vulnerabilities.outputs.vulnerabilities_found }},
              "critical": ${{ needs.scan-vulnerabilities.outputs.critical_count }},
              "high": ${{ needs.scan-vulnerabilities.outputs.high_count }},
              "medium": ${{ needs.scan-vulnerabilities.outputs.medium_count }},
              "low": ${{ needs.scan-vulnerabilities.outputs.low_count }}
            },
            "sast": {
              "total": ${{ needs.scan-sast.outputs.sast_issues_found }},
              "critical": ${{ needs.scan-sast.outputs.sast_critical }},
              "high": ${{ needs.scan-sast.outputs.sast_high }},
              "medium": ${{ needs.scan-sast.outputs.sast_medium }},
              "low": ${{ needs.scan-sast.outputs.sast_low }}
            },
            "secrets": {
              "found": ${{ needs.scan-secrets.outputs.secrets_found }}
            },
            "metadata": {
              "version": "${{ needs.prepare.outputs.version }}",
              "commit": "${{ github.sha }}",
              "build_time": "${{ needs.prepare.outputs.build_start_time }}"
            }
          }
          EOF

      - name: "🔒 Evaluate Security Policy"
        id: opa_eval
        run: |
          # [OPA evaluation logic remains identical to original]
          # Copy the entire OPA evaluation block from your original
          # policy-enforcement job here
          set -e
          echo ""
          echo "🛡️ ===== SECURITY POLICY EVALUATION ====="
          echo ""
          opa eval -i input.json -d ./policy 'data.main' --format=pretty 2>&1 || { echo "❌ OPA evaluation failed!"; exit 1; }
          ALLOW_RESULT=$(opa eval -i input.json -d ./policy 'data.main.allow' --format=json 2>&1)
          PASSED=$(echo "$ALLOW_RESULT" | jq -r '.result[0].expressions[0].value // false' | tr -d '[:space:]')
          if [ -z "$PASSED" ] || [ "$PASSED" = "null" ]; then echo "❌ ERROR: Could not evaluate allow rule!"; exit 1; fi
          echo "passed=${PASSED}" >> $GITHUB_OUTPUT
          VIOLATIONS_RESULT=$(opa eval -i input.json -d ./policy 'data.main.deny' --format=json 2>/dev/null || echo '{"result":[{"expressions":[{"value":[]}]}]}')
          VIOLATION_COUNT=$(echo "$VIOLATIONS_RESULT" | jq -r '.result[0].expressions[0].value | length' | tr -d '[:space:]')
          if ! [[ "$VIOLATION_COUNT" =~ ^[0-9]+$ ]]; then VIOLATION_COUNT=0; fi
          echo "violations=${VIOLATION_COUNT}" >> $GITHUB_OUTPUT
          WARNINGS_RESULT=$(opa eval -i input.json -d ./policy 'data.main.warnings' --format=json 2>/dev/null || echo '{"result":[{"expressions":[{"value":[]}]}]}')
          WARNING_COUNT=$(echo "$WARNINGS_RESULT" | jq -r '.result[0].expressions[0].value | length' | tr -d '[:space:]')
          if ! [[ "$WARNING_COUNT" =~ ^[0-9]+$ ]]; then WARNING_COUNT=0; fi
          COMPLIANCE=$(opa eval -i input.json -d ./policy 'data.main.compliance_report' --format=json 2>/dev/null || echo '{"result":[{"expressions":[{"value":{}}]}]}')
          COMPLIANCE_DATA=$(echo "$COMPLIANCE" | jq -r '.result[0].expressions[0].value // {}')
          echo ""
          echo "📊 ===== POLICY EVALUATION RESULTS ====="
          echo "   Compliance Status: ${PASSED}"
          echo "   Violations: ${VIOLATION_COUNT}"
          echo "   Warnings: ${WARNING_COUNT}"
          echo "========================================="
          echo ""
          if [ "$VIOLATION_COUNT" -gt 0 ]; then
            echo "❌ POLICY VIOLATIONS DETECTED:"
            echo "$VIOLATIONS_RESULT" | jq -r '.result[0].expressions[0].value[] | "   ❌ [\(.type)] \(.msg)"' 2>/dev/null || echo "   (Unable to parse violation details)"
            echo ""
          fi
          if [ "$WARNING_COUNT" -gt 0 ]; then
            echo "⚠️  POLICY WARNINGS:"
            echo "$WARNINGS_RESULT" | jq -r '.result[0].expressions[0].value[] | "   ⚠️  [\(.type)] \(.msg)"' 2>/dev/null || echo "   (Unable to parse warning details)"
            echo ""
          fi
          echo "📋 DETAILED COMPLIANCE REPORT:"
          echo "$COMPLIANCE_DATA" | jq '.' 2>/dev/null || echo "{}"
          echo ""
          if [ "$PASSED" = "true" ]; then
            echo "decision=APPROVED" >> $GITHUB_OUTPUT
            echo "✅ ===== POLICY CHECK PASSED ====="
            echo "   The image meets all security requirements"
            echo "   and can proceed to deployment."
            echo "===================================="
            exit 0
          else
            echo "decision=REJECTED" >> $GITHUB_OUTPUT
            echo "❌ ===== POLICY CHECK FAILED ====="
            echo "   The image does NOT meet security requirements."
            echo "   Deployment is BLOCKED."
            echo ""
            echo "   📊 Summary:"
            echo "   - CVE Critical: $(echo "$COMPLIANCE_DATA" | jq -r '.cve.critical // 0')"
            echo "   - CVE High: $(echo "$COMPLIANCE_DATA" | jq -r '.cve.high // 0')"
            echo "   - SAST Critical: $(echo "$COMPLIANCE_DATA" | jq -r '.sast.critical // 0')"
            echo "   - SAST High: $(echo "$COMPLIANCE_DATA" | jq -r '.sast.high // 0')"
            echo ""
            echo "   Review Stage 3 logs for detailed security findings."
            echo "===================================="
            exit 1
          fi

      - name: "⏱️ Record Policy Check End"
        id: policy-end
        if: always()
        run: echo "time=$(date +%s)" >> $GITHUB_OUTPUT

      - name: "💾 Upload Policy Evaluation Results"
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: policy-evaluation
          path: input.json
          retention-days: 90

    # ============================================
  # Stage 5: Metrics & Reporting
  # ============================================
  metrics-and-report:
    name: "5️⃣ Generate Metrics & Security Report"
    runs-on: ubuntu-latest
    needs: [prepare, build-container, scan-vulnerabilities, scan-sast, scan-secrets, policy-enforcement]
    if: always()
    permissions:
      contents: read

    steps:
      - name: "📋 Checkout Repository"
        uses: actions/checkout@v4

      - name: "📊 Calculate Pipeline Metrics"
        id: calculate
        run: |
          BUILD_START=${{ needs.prepare.outputs.build_start_time || '0' }}
          POLICY_START=${{ needs.policy-enforcement.outputs.policy_start_time || '0' }}
          POLICY_END=${{ needs.policy-enforcement.outputs.policy_end_time || '0' }}
          CURRENT_TIME=$(date +%s)
          
          if [ "$BUILD_START" != "0" ] && [ "$POLICY_START" != "0" ] && [ "$POLICY_END" != "0" ]; then
            POLICY_DURATION=$((POLICY_END - POLICY_START))
            TOTAL_DURATION=$((CURRENT_TIME - BUILD_START))
          else
            POLICY_DURATION=0
            TOTAL_DURATION=0
          fi
          
          echo "policy_duration=${POLICY_DURATION}" >> $GITHUB_OUTPUT
          echo "total_duration=${TOTAL_DURATION}" >> $GITHUB_OUTPUT

      - name: "📊 Generate Security Report"
        run: |
          mkdir -p reports
          
          cat > reports/security-report-${{ github.run_number }}.md << 'EOF'
          # 🔒 Software Supply Chain Security Report
          
          **Pipeline Run:** #${{ github.run_number }}  
          **Date:** $(date -u +"%Y-%m-%d %H:%M:%S UTC")  
          **Version:** ${{ needs.prepare.outputs.version }}  
          **Commit:** `${{ github.sha }}`
          
          ---
          
          ## 📦 SBOM Analysis
          - **Components:** ${{ needs.scan-vulnerabilities.outputs.sbom_components_count || 0 }}
          - **Format:** CycloneDX JSON
          
          ## 🔍 CVE Vulnerabilities (Dependencies)
          | Severity | Count |
          |----------|-------|
          | 🔴 Critical | ${{ needs.scan-vulnerabilities.outputs.critical_count || 0 }} |
          | 🟠 High | ${{ needs.scan-vulnerabilities.outputs.high_count || 0 }} |
          | 🟡 Medium | ${{ needs.scan-vulnerabilities.outputs.medium_count || 0 }} |
          | 🟢 Low | ${{ needs.scan-vulnerabilities.outputs.low_count || 0 }} |
          | **Total** | **${{ needs.scan-vulnerabilities.outputs.vulnerabilities_found || 0 }}** |
          
          ## 🔬 Code Security Issues (SAST - Semgrep)
          | Severity | Count |
          |----------|-------|
          | 🔴 Critical (ERROR) | ${{ needs.scan-sast.outputs.sast_critical || 0 }} |
          | 🟠 High (WARNING) | ${{ needs.scan-sast.outputs.sast_high || 0 }} |
          | 🟡 Medium (INFO) | ${{ needs.scan-sast.outputs.sast_medium || 0 }} |
          | 🟢 Low (NOTE) | ${{ needs.scan-sast.outputs.sast_low || 0 }} |
          | **Total** | **${{ needs.scan-sast.outputs.sast_issues_found || 0 }}** |
          
          ## 🔐 Secrets Detection (TruffleHog)
          | Type | Count |
          |------|-------|
          | 🔑 Secrets Detected | ${{ needs.scan-secrets.outputs.secrets_found || 0 }} |
          
          ## 🛡️ Policy Enforcement
          - **Status:** ${{ needs.policy-enforcement.outputs.policy_result == 'true' && '✅ PASSED' || '❌ FAILED' }}
          - **Decision:** `${{ needs.policy-enforcement.outputs.policy_decision || 'UNKNOWN' }}`
          - **Violations:** ${{ needs.policy-enforcement.outputs.policy_violations || 0 }}
          
          ## 🏆 SLSA Level 3 Compliance
          | Requirement | Status |
          |-------------|--------|
          | Provenance | ✅ Non-falsifiable |
          | Build System | ✅ Hermetic |
          | Signing | ✅ Keyless Cosign |
          | SBOM | ✅ CycloneDX |
          | Policy | ✅ OPA Enforced |
          
          ## ⏱️ Performance
          - **Policy Check:** ${{ steps.calculate.outputs.policy_duration }}s
          - **Total Pipeline:** ${{ steps.calculate.outputs.total_duration }}s
          
          ## 🐳 Artifact
          - **Image:** `${{ needs.build-container.outputs.image_ref }}`
          
          ---
          *Automated Software Supply Chain Security Framework*
          EOF
          
          echo "✅ Security report generated"

      - name: "💾 Upload Reports"
        uses: actions/upload-artifact@v4
        with:
          name: security-report
          path: reports/
          retention-days: 90

      - name: "📊 Pipeline Summary"
        if: always()
        run: |
          echo "# 🏆 SLSA Level 3 Supply Chain Security Pipeline" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Mission:** Automating Software Supply Chain Security with SBOM and SLSA Compliance" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "## 📦 Build Information" >> $GITHUB_STEP_SUMMARY
          echo "- **Version:** \`${{ needs.prepare.outputs.version }}\`" >> $GITHUB_STEP_SUMMARY
          echo "- **Image:** \`${{ needs.build-container.outputs.image_ref }}\`" >> $GITHUB_STEP_SUMMARY
          echo "- **Commit:** \`${{ github.sha }}\`" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "## 📋 SBOM Analysis" >> $GITHUB_STEP_SUMMARY
          echo "- **Components:** ${{ needs.scan-vulnerabilities.outputs.sbom_components_count || 0 }}" >> $GITHUB_STEP_SUMMARY
          echo "- **Format:** CycloneDX JSON" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "## 🔍 Security Scan Results" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### CVE Vulnerabilities (Dependencies)" >> $GITHUB_STEP_SUMMARY
          echo "| Severity | Count |" >> $GITHUB_STEP_SUMMARY
          echo "|----------|-------|" >> $GITHUB_STEP_SUMMARY
          echo "| 🔴 Critical | ${{ needs.scan-vulnerabilities.outputs.critical_count || 0 }} |" >> $GITHUB_STEP_SUMMARY
          echo "| 🟠 High | ${{ needs.scan-vulnerabilities.outputs.high_count || 0 }} |" >> $GITHUB_STEP_SUMMARY
          echo "| 🟡 Medium | ${{ needs.scan-vulnerabilities.outputs.medium_count || 0 }} |" >> $GITHUB_STEP_SUMMARY
          echo "| 🟢 Low | ${{ needs.scan-vulnerabilities.outputs.low_count || 0 }} |" >> $GITHUB_STEP_SUMMARY
          echo "| **Total** | **${{ needs.scan-vulnerabilities.outputs.vulnerabilities_found || 0 }}** |" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### Code Security Issues (SAST - Semgrep)" >> $GITHUB_STEP_SUMMARY
          echo "| Severity | Count |" >> $GITHUB_STEP_SUMMARY
          echo "|----------|-------|" >> $GITHUB_STEP_SUMMARY
          echo "| 🔴 Critical (ERROR) | ${{ needs.scan-sast.outputs.sast_critical || 0 }} |" >> $GITHUB_STEP_SUMMARY
          echo "| 🟠 High (WARNING) | ${{ needs.scan-sast.outputs.sast_high || 0 }} |" >> $GITHUB_STEP_SUMMARY
          echo "| 🟡 Medium (INFO) | ${{ needs.scan-sast.outputs.sast_medium || 0 }} |" >> $GITHUB_STEP_SUMMARY
          echo "| 🟢 Low (NOTE) | ${{ needs.scan-sast.outputs.sast_low || 0 }} |" >> $GITHUB_STEP_SUMMARY
          echo "| **Total** | **${{ needs.scan-sast.outputs.sast_issues_found || 0 }}** |" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### Secrets Detection (TruffleHog)" >> $GITHUB_STEP_SUMMARY
          echo "| Type | Count |" >> $GITHUB_STEP_SUMMARY
          echo "|------|-------|" >> $GITHUB_STEP_SUMMARY
          echo "| 🔐 Secrets Detected | ${{ needs.scan-secrets.outputs.secrets_found || 0 }} |" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "## 🛡️ Policy Enforcement" >> $GITHUB_STEP_SUMMARY
          echo "- **Status:** ${{ needs.policy-enforcement.outputs.policy_result == 'true' && '✅ PASSED' || '❌ FAILED' }}" >> $GITHUB_STEP_SUMMARY
          echo "- **Decision:** \`${{ needs.policy-enforcement.outputs.policy_decision || 'UNKNOWN' }}\`" >> $GITHUB_STEP_SUMMARY
          echo "- **Violations:** ${{ needs.policy-enforcement.outputs.policy_violations || 0 }}" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "## 🏆 SLSA Level 3 Compliance" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ **Provenance:** Non-falsifiable (Sigstore)" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ **Build System:** Hermetic (Docker Buildx)" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ **Signing:** Keyless with Cosign" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ **SBOM:** CycloneDX attached" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ **Policy:** OPA enforcement" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "## ⏱️ Performance" >> $GITHUB_STEP_SUMMARY
          echo "- **Total Duration:** ${{ steps.calculate.outputs.total_duration }}s" >> $GITHUB_STEP_SUMMARY
          echo "- **Policy Check:** ${{ steps.calculate.outputs.policy_duration }}s" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "---" >> $GITHUB_STEP_SUMMARY
          echo "*Automated Software Supply Chain Security Framework*" >> $GITHUB_STEP_SUMMARY